import { Heading, Stack } from '@/libs/chakra';

const SignUpEmailForm = () => {
  return (
    <Stack p={8} gap={6}>
      <Heading size="xl" textAlign="center">
        新規登録
      </Heading>
    </Stack>
  );
};

export default SignUpEmailForm;
